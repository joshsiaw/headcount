$("table.new-attendees").replaceWith('<%= escape_javascript(render("events/add_attendees", event: @event, new_attendees: @new_attendees)) %>');
$("table.attendees").replaceWith('<%= escape_javascript(render("events/attendees", event: @event)) %>');